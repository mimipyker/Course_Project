<Window x:Class="FinanceManagerApp.Views.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавить транзакцию" Height="360" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Сумма:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="AmountTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Категория:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox x:Name="CategoryComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" IsEditable="False"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Тип:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" IsEditable="False"
                  SelectionChanged="TypeComboBox_SelectionChanged"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Дата:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <DatePicker x:Name="DatePicker" Grid.Row="4" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="5" Grid.ColumnSpan="2"
                   Foreground="Tomato"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <Button Grid.Row="6" Grid.ColumnSpan="2"
                Content="Добавить"
                HorizontalAlignment="Right"
                Width="120"
                Padding="10,6"
                Click="Add_Click"/>
    </Grid>
</Window>

