<Window x:Class="FinanceManagerApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Finance Manager" Height="520" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Учет личных финансов"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" LastChildFill="False" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="Общий доход:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="IncomeTextBlock" Text="0" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="Общий расход:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ExpenseTextBlock" Text="0" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Итоговый баланс:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="BalanceTextBlock" Text="0" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Добавить" Width="110" Margin="0,0,8,0" Padding="10,6" Click="AddButton_Click"/>
                <Button Content="Удалить" Width="110" Margin="0,0,8,0" Padding="10,6" Click="DeleteButton_Click"/>
                <Button Content="Сохранить" Width="110" Padding="10,6" Click="SaveButton_Click"/>
            </StackPanel>
        </DockPanel>

        <DockPanel Grid.Row="2" Margin="0,0,0,10" LastChildFill="True">
            <TextBlock Text="Поиск" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <TextBox x:Name="SearchTextBox" Height="28" VerticalContentAlignment="Center"
                     TextChanged="SearchTextBox_TextChanged"/>
        </DockPanel>

        <DataGrid x:Name="TransactionsDataGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  CanUserReorderColumns="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="4" Foreground="#666">
            Данные сохраняются в файл data.json рядом с приложением.
        </TextBlock>
    </Grid>
</Window>

